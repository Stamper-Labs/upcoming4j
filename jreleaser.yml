project:
  name: upcoming4j
  version: 0.0.1
  versionPattern: SEMVER
  authors: [Captain Mcwiise]
  license: Apache-2.0
  links:
    homepage: https://stamperlabs.io/upcoming4j
  inceptionYear: 2026

release:
  github:
    enabled: true
    owner: Stamper-Labs
    token: "{{ env.UPCOMING4J_GITHUB_TOKEN }}"
    skipRelease: false
    changelog:
      enabled: true
      formatted: ALWAYS
      preset: conventional-commits
      format: '- {{commitShortHash}} {{commitTitle}}'